/**
 * `rgbString` returns a rgb string format. `rgb(0, 0, 0)`
 *
 * `hex` returns a color in hexadecimal format. `#000000`
 *
 * `rgbArray` returns a array of rgb. `[0, 0, 0]`
 *
 * `hslString` returns an hsl string format. `hsl(0, 0, 0)`
 *
 * `hslArray` returns a array of hsl. `[0, 0, 0]`
 *
 * `keyword` returns CSS color keyword (https://drafts.csswg.org/css-color/#named-colors)
 */
export declare type ColorFormats = 'rgbString' | 'hex' | 'rgbArray' | 'hslString' | 'hslArray' | 'keyword';
